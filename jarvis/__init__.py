"""Jarvis: a voice-navigable, context-aware assistant for the garage.

This package currently implements the *workspace system*: the layer that lets
Jarvis stand inside a named context ("project cressida"), keep track of what it
is focused on ("my current engine"), and answer questions from that context's
knowledge. Voice capture (microphones) and vision (cameras) are future layers
that feed into this same core.
"""

__version__ = "0.0.1"
